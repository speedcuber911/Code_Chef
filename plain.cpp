#include<iomanip>
#include<iostream>
#include<vector>
#include<algorithm>
#include<string>
#include<map>
#include<math.h>

using namespace std;

int main()
{
  int t;
  cin>>t;
  while(t--)
  {
    vector<int>a;
    char b;
    cin>>b;
    a.push_back(int(b)-48);
    if(a.size()%2!=0)
    {
      if(a[a.size()/2]!=)
    }
    for(int i=0;i<a.length();i++)
    {
      if()
    }
  }
  return 0;
}
